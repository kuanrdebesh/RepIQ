# Supabase Notes

Supabase is the system of record for:

- auth identity
- durable training history
- privacy settings
- shareability primitives for future network and groups features

The initial migration includes V1 tables plus the minimum future-facing tables the product doc calls out for V2 readiness.

